# reportePCB

Reporte generado en powershell

# En javascript se usa FileReader en vez de fetch() porque fetch() 
# a un archivo local (file://) queda bloqueado por CORS.
  
  Json no importa directamente o automaticamente el json al HTML y JS ya que sale error por bloqueo por cors
  